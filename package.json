{
  "name": "agenda-contactos",
  "version": "1.0.0",
  "private": true,
  "description": "Agenda web para registrar, buscar y eliminar contactos",
  "scripts": {
    "test": "node --test",
    "verificar": "node --check script.js && node --check utilidades.js"
  },
  "engines": {
    "node": ">=20"
  }
}
